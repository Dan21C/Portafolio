import { Component } from 'react';
export default class ErrorBoundary extends Component { constructor(props){super(props);this.state={failed:false}} static getDerivedStateFromError(){return{failed:true}} componentDidCatch(error,info){console.error('APX UI rendering failure',{name:error?.name,componentStack:info?.componentStack})} render(){if(this.state.failed)return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:'2rem',textAlign:'center'}}><div><h1>No pudimos mostrar esta página.</h1><p>Recarga para intentarlo nuevamente.</p><button onClick={()=>window.location.reload()}>Recargar</button></div></main>;return this.props.children} }
