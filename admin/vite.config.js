import { defineConfig, loadEnv } from 'vite'; import react from '@vitejs/plugin-react'; import { resolve } from 'node:path'; import { validateFrontendProductionEnv } from '../build/validateFrontendEnv.js';
export default defineConfig(({mode})=>{const root=resolve(import.meta.dirname);const envDir=resolve(import.meta.dirname,'..');const env=loadEnv(mode,envDir,'');validateFrontendProductionEnv(mode,env,'APX Admin');return { root, envDir, base: '/admin/', plugins:[react()], build:{ outDir: resolve(import.meta.dirname,'../dist/admin'), emptyOutDir:false } }});
